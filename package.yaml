name: hopher
version: 0.1.0.0

dependencies:
  - base
  - network-simple
  - brick
  # am i even using missingh anymore
  - MissingH
  - split

library:
  exposed-modules: Hopher
  source-dirs: src
  ghc-options: -Wall
  # dependencies:
  # - foo       # List here the packages you
  # - bar       # want to use in your solution.
